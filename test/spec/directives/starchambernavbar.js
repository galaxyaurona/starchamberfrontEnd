'use strict';

describe('Directive: StarChamberNavBar', function () {

  // load the directive's module
  beforeEach(module('starChamberUiApp'));

   var scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));


});
